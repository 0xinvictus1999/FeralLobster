version: '3.8'

# =============================================================================
# Axobase - 去中心化 AI 放养平台
# Docker Compose 配置
# ⚠️ Base Sepolia Testnet Only - 禁止主网操作
# =============================================================================

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: axo_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: axo
      POSTGRES_USER: axo
      POSTGRES_PASSWORD: axo_testnet_only
    volumes:
      - axo_postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U axo -d axo"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - axo-network

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: axo_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - axo_redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - axo-network

  # Orchestrator API
  orchestrator:
    build:
      context: ./orchestrator
      dockerfile: Dockerfile
    container_name: axo_orchestrator
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://axo:axo_testnet_only@postgres:5432/axo
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    env_file:
      - ./orchestrator/.env
    volumes:
      - ./orchestrator/data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - axo-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Telegram Bot
  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    container_name: axo_bot
    restart: unless-stopped
    environment:
      - PLATFORM_API_URL=http://orchestrator:8000
    env_file:
      - ./bot/.env
    depends_on:
      - orchestrator
    networks:
      - axo-network

  # Web Frontend
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: axo_web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_PLATFORM_API_URL=http://localhost:8000
    env_file:
      - ./web/.env.local
    depends_on:
      - orchestrator
    networks:
      - axo-network

  # Celery Worker (后台任务)
  celery:
    build:
      context: ./orchestrator
      dockerfile: Dockerfile
    container_name: axo_celery
    restart: unless-stopped
    command: celery -A tasks worker --loglevel=info
    environment:
      - DATABASE_URL=postgresql://axo:axo_testnet_only@postgres:5432/axo
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    env_file:
      - ./orchestrator/.env
    depends_on:
      - postgres
      - redis
      - orchestrator
    networks:
      - axo-network

  # Celery Beat (定时任务调度器)
  celery-beat:
    build:
      context: ./orchestrator
      dockerfile: Dockerfile
    container_name: axo_celery_beat
    restart: unless-stopped
    command: celery -A tasks beat --loglevel=info
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    env_file:
      - ./orchestrator/.env
    depends_on:
      - redis
      - celery
    networks:
      - axo-network

volumes:
  axo_postgres_data:
    driver: local
  axo_redis_data:
    driver: local

networks:
  axo-network:
    driver: bridge
